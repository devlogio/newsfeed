class NewsArticle {
  String title;
  String author;
  String publishedDate;
  String imageURL;
  String articleURL;

  NewsArticle(this.title, this.author, this.publishedDate, this.imageURL, this.articleURL);
}
