class NewsArticle {
  String topic;
  String title;
  String author;
  String publishedDate;
  String imageURL;
  String articleURL;

  NewsArticle(this.topic, this.title, this.author, this.publishedDate, this.imageURL, this.articleURL);
}
